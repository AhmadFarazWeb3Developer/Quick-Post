<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Edit Post</title>
  </head>

  <body class="bg-zinc-900 text-zinc-50 font-sans min-h-screen">
    <!-- Navbar -->
    <header class="w-full py-4 px-6 bg-zinc-800 shadow-md">
      <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-lg font-bold tracking-wide text-blue-400">QuickPost</h1>
        <a
          href="/logout"
          class="bg-blue-800 px-4 py-2 rounded-md text-sm hover:bg-blue-700"
        >
          Logout
        </a>
      </div>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-6 py-8">
      <!-- Welcome Message -->
      <section class="mb-8">
        <h2 class="text-3xl font-semibold text-zinc-200 mb-2">
          Edit Your Post 📝
        </h2>
        <p class="text-zinc-400 text-sm">
          Make the necessary changes and update your post.
        </p>
      </section>

      <!-- Edit Post Form -->
      <section class="bg-zinc-800 p-6 rounded-md shadow-md mb-8">
        <h3 class="text-lg font-medium text-zinc-200 mb-4">Update Post</h3>
        <form
          action="/update/<%= post._id %>"
          method="post"
          class="flex flex-col gap-4"
        >
          <textarea
            name="content"
            class="w-full h-28 p-4 text-zinc-200 bg-zinc-900 border border-zinc-700 rounded-md focus:ring-2 focus:ring-blue-500 outline-none resize-none"
          >
<%= post.content %></textarea
          >

          <input
            type="submit"
            class="self-end px-6 py-2 bg-blue-600 text-sm font-semibold rounded-md hover:bg-blue-500 cursor-pointer"
            value="Update Post"
          />
        </form>
      </section>
    </main>
  </body>
</html>
 